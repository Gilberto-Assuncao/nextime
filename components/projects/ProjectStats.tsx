import type { Project } from "@/lib/types/project";
export default function ProjectStats({ project }: { project: Project }) { const items = [["Tracked hours", `${project.statistics.trackedHours}h`], ["Billable hours", `${project.statistics.billableHours}h`], ["Time entries", project.statistics.entries], ["Members", project.statistics.members]]; return <dl className="grid grid-cols-2 gap-3 lg:grid-cols-4">{items.map(([label, value]) => <div key={label} className="rounded-xl border border-white/10 bg-[#161A34] p-4"><dt className="text-xs text-[#9CA3AF]">{label}</dt><dd className="mt-1 text-xl font-bold text-[#E5E7EB]">{value}</dd></div>)}</dl>; }
