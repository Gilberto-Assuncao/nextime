import TimerDisplay from "@/components/time/TimerDisplay";
import type { RunningSession } from "@/lib/types/time";

export default function CurrentSessionCard({ session }: { session: RunningSession }) {
  return <section aria-labelledby="current-session-title" className="rounded-2xl border border-[#22C55E]/25 bg-gradient-to-br from-[#22C55E]/10 to-[#161A34] p-5 sm:p-6"><div className="flex items-center justify-between gap-3"><h3 id="current-session-title" className="text-lg font-semibold text-[#E5E7EB]">Current Session</h3><span className="inline-flex items-center gap-2 rounded-full bg-[#22C55E]/10 px-3 py-1 text-xs font-semibold text-[#4ADE80]"><span className="h-2 w-2 rounded-full bg-[#22C55E]" aria-hidden="true" />In progress</span></div><dl className="mt-5 grid gap-4 sm:grid-cols-3"><div><dt className="text-xs text-[#9CA3AF]">Project</dt><dd className="mt-1 text-sm font-medium text-[#E5E7EB]">{session.project.name}</dd></div><div><dt className="text-xs text-[#9CA3AF]">Task</dt><dd className="mt-1 text-sm font-medium text-[#E5E7EB]">{session.task.name}</dd></div><div><dt className="text-xs text-[#9CA3AF]">Started at</dt><dd className="mt-1 text-sm font-medium text-[#E5E7EB]">{session.startedAt}</dd></div></dl><div className="mt-6 border-t border-white/10 pt-6"><TimerDisplay seconds={session.elapsedSeconds} /></div></section>;
}
