import { useEffect, useRef, type FormEvent } from "react";
import type { TimesheetEntry } from "@/lib/types/timesheet";

const field = "mt-2 min-h-11 w-full rounded-lg border border-white/10 bg-[#111827] px-3 text-sm text-[#E5E7EB] outline-none focus:border-[#22C55E] focus:ring-2 focus:ring-[#22C55E]/20";
export default function EditEntryModal({ entry, projects, onClose, onSave }: { entry: TimesheetEntry; projects: string[]; onClose: () => void; onSave: () => void }) { const closeRef = useRef<HTMLButtonElement>(null); useEffect(() => { closeRef.current?.focus(); const closeOnEscape = (event: KeyboardEvent) => { if (event.key === "Escape") onClose(); }; document.addEventListener("keydown", closeOnEscape); return () => document.removeEventListener("keydown", closeOnEscape); }, [onClose]); const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); onSave(); }; return <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/70 p-0 backdrop-blur-sm sm:items-center sm:p-4" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose(); }}><section role="dialog" aria-modal="true" aria-labelledby="edit-entry-title" className="max-h-[92dvh] w-full overflow-y-auto rounded-t-2xl border border-white/10 bg-[#161A34] p-5 shadow-2xl sm:max-w-2xl sm:rounded-2xl sm:p-6"><div className="flex items-center justify-between gap-4"><div><h3 id="edit-entry-title" className="text-xl font-semibold text-[#E5E7EB]">Edit Entry</h3><p className="mt-1 text-xs text-[#9CA3AF]">{entry.employee} · {entry.date}</p></div><button ref={closeRef} type="button" onClick={onClose} aria-label="Close edit entry" className="flex min-h-11 min-w-11 items-center justify-center rounded-lg text-xl text-[#9CA3AF] hover:bg-white/5 focus-visible:outline-2 focus-visible:outline-[#22C55E]">×</button></div><form onSubmit={submit} className="mt-6 grid gap-4"><div className="grid gap-4 sm:grid-cols-2"><div><label htmlFor="edit-project" className="text-sm font-medium text-[#D1D5DB]">Project</label><select id="edit-project" defaultValue={entry.project} className={field}>{projects.map((project) => <option key={project}>{project}</option>)}</select></div><div><label htmlFor="edit-task" className="text-sm font-medium text-[#D1D5DB]">Task</label><input id="edit-task" defaultValue={entry.task} required className={field} /></div><div><label htmlFor="edit-start" className="text-sm font-medium text-[#D1D5DB]">Start</label><input id="edit-start" type="time" defaultValue={entry.startTime} required className={`${field} [color-scheme:dark]`} /></div><div><label htmlFor="edit-end" className="text-sm font-medium text-[#D1D5DB]">End</label><input id="edit-end" type="time" defaultValue={entry.endTime} required className={`${field} [color-scheme:dark]`} /></div><div><label htmlFor="edit-break" className="text-sm font-medium text-[#D1D5DB]">Break (minutes)</label><input id="edit-break" type="number" min="0" max="480" defaultValue={entry.breakMinutes} required className={field} /></div></div><div><label htmlFor="edit-notes" className="text-sm font-medium text-[#D1D5DB]">Notes</label><textarea id="edit-notes" defaultValue={entry.notes} maxLength={300} rows={3} className={`${field} py-3`} /></div><div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button type="button" onClick={onClose} className="min-h-11 rounded-lg border border-white/15 px-5 text-sm font-semibold text-[#E5E7EB] hover:bg-white/5 focus-visible:outline-2 focus-visible:outline-[#22C55E]">Cancel</button><button type="submit" className="min-h-11 rounded-lg bg-[#22C55E] px-5 text-sm font-semibold text-[#07110B] hover:bg-[#16A34A] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#22C55E]">Save</button></div></form></section></div>; }
