import ApprovalStatusBadge from "@/components/timesheets/ApprovalStatusBadge";
import type { TimesheetEntry } from "@/lib/types/timesheet";

export default function TimesheetTable({ entries, onEdit }: { entries: TimesheetEntry[]; onEdit: (entry: TimesheetEntry) => void }) { return <div className="hidden overflow-hidden rounded-2xl border border-white/10 bg-[#161A34] md:block"><table className="w-full table-fixed text-left text-sm"><caption className="sr-only">Weekly timesheet entries</caption><thead className="bg-white/[0.03] text-xs uppercase tracking-wide text-[#9CA3AF]"><tr>{[["Date","w-[10%]"],["Project","w-[15%]"],["Task","w-[15%]"],["Start","w-[7%]"],["End","w-[7%]"],["Break","w-[7%]"],["Worked","w-[8%]"],["Status","w-[11%]"],["Notes","w-[15%]"],["","w-[5%]"]].map(([label,width]) => <th key={label || "actions"} scope="col" className={`${width} px-3 py-4 font-semibold`}>{label}<span className="sr-only">{label ? "" : "Actions"}</span></th>)}</tr></thead><tbody className="divide-y divide-white/10">{entries.map((entry) => <tr key={entry.id} className="transition hover:bg-white/[0.025]"><td className="px-3 py-4 text-[#D1D5DB]">{entry.date}</td><td className="truncate px-3 py-4 font-medium text-[#E5E7EB]" title={entry.project}>{entry.project}</td><td className="truncate px-3 py-4 text-[#D1D5DB]" title={entry.task}>{entry.task}</td><td className="px-3 py-4 text-[#D1D5DB]">{entry.startTime}</td><td className="px-3 py-4 text-[#D1D5DB]">{entry.endTime}</td><td className="px-3 py-4 text-[#D1D5DB]">{entry.breakMinutes}m</td><td className="px-3 py-4 font-semibold text-[#4ADE80]">{Math.floor(entry.workedMinutes / 60)}h {entry.workedMinutes % 60}m</td><td className="px-3 py-4"><ApprovalStatusBadge status={entry.status} /></td><td className="truncate px-3 py-4 text-[#9CA3AF]" title={entry.notes}>{entry.notes}</td><td className="px-3 py-4"><button type="button" onClick={() => onEdit(entry)} aria-label={`Edit ${entry.project} entry for ${entry.date}`} aria-haspopup="dialog" className="flex min-h-11 min-w-11 items-center justify-center rounded-lg text-[#9CA3AF] hover:bg-white/5 hover:text-[#E5E7EB] focus-visible:outline-2 focus-visible:outline-[#22C55E]">✎</button></td></tr>)}</tbody></table></div>; }
