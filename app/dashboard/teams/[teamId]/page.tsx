import type{Metadata}from"next";import{notFound}from"next/navigation";import{getTeamDetails,TeamDetailsView}from"@/src/features/teams";export const metadata:Metadata={title:"Team details"};export default async function TeamPage({params}:{params:Promise<{teamId:string}>}){const{teamId}=await params;const team=await getTeamDetails(teamId);if(!team)notFound();return <TeamDetailsView team={team}/>}
