import type { Metadata } from "next"; import Link from "next/link"; import PageHeader from "@/components/dashboard/PageHeader"; import ProjectsTable from "@/components/projects/ProjectsTable"; import { getProjects } from "@/src/features/projects/data";
export const metadata: Metadata = { title: "Projects" };
export default async function ProjectsPage() { const { projects, clients } = await getProjects(); return <section aria-labelledby="projects-heading"><PageHeader headingId="projects-heading" eyebrow="Workspace" title="Projects" description={`${projects.length} projects across ${clients.length} clients. Track delivery, hours, budgets, and team progress.`} actions={<Link href="/dashboard/projects/new" className="flex min-h-11 items-center justify-center rounded-lg bg-[#22C55E] px-5 text-sm font-semibold text-[#07110B] hover:bg-[#16A34A] focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[#22C55E]">Add Project</Link>}/><ProjectsTable projects={projects} clients={clients}/></section>; }
