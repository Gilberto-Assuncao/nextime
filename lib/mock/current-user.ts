import type { CurrentUser } from "@/lib/types/user";

// Replaced by the authenticated user session when backend integration is added.
export const currentUser: CurrentUser = { name: "Gilberto Assunção", firstName: "Gilberto", role: "Administrator", initials: "GA" };
