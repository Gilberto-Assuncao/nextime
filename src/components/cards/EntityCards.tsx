import type { ReactNode } from "react"; import { Badge } from "@/src/components/data-display"; import { componentClasses } from "@/src/design-system";
export interface EntityCardProps { title: string; subtitle?: string; status?: string; metadata?: ReactNode; action?: ReactNode; children?: ReactNode }
export function EntityCard({ title, subtitle, status, metadata, action, children }: EntityCardProps) { return <article className={`${componentClasses.surface} p-5`}><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold text-[#E5E7EB]">{title}</h3>{subtitle ? <p className="mt-1 text-sm text-[#9CA3AF]">{subtitle}</p> : null}</div>{status ? <Badge>{status}</Badge> : null}</div>{metadata ? <div className="mt-4 text-sm text-[#D1D5DB]">{metadata}</div> : null}{children ? <div className="mt-4">{children}</div> : null}{action ? <div className="mt-5">{action}</div> : null}</article>; }
export const CompanyCard = EntityCard; export const ProfessionalCard = EntityCard; export const ProjectCard = EntityCard; export const ChantierCard = EntityCard; export const ReportCard = EntityCard; export const NotificationCard = EntityCard; export const CertificateCard = EntityCard;
