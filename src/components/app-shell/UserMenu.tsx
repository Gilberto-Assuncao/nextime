import Link from "next/link"; import type { AppUserSummary } from "./types";
import { signOutAction } from "@/app/auth/actions";
export function UserMenu({ user }: { user:AppUserSummary }) { return <details className="relative"><summary aria-label="Open user menu" className="flex min-h-11 cursor-pointer list-none items-center gap-2 rounded-lg px-1 sm:px-2 focus-visible:outline-2 focus-visible:outline-[#22C55E]"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-[#22C55E]/10 text-xs font-bold text-[#22C55E]">{user.initials}</span><span className="hidden text-left xl:block"><span className="block text-sm font-semibold text-[#E5E7EB]">{user.name}</span><span className="block text-xs text-[#9CA3AF]">{user.email}</span></span></summary><div className="absolute right-0 z-30 mt-2 w-56 rounded-xl border border-white/10 bg-[#161A34] p-2 shadow-xl">{[["Profile","#"],["Settings","/dashboard/settings"],["Support","#"]].map(([label,href])=><Link key={label} href={href} className="flex min-h-11 items-center rounded-lg px-3 text-sm text-[#D1D5DB] hover:bg-white/5 focus-visible:outline-2 focus-visible:outline-[#22C55E]">{label}</Link>)}<form action={signOutAction}><button type="submit" className="flex min-h-11 w-full items-center rounded-lg px-3 text-sm text-[#D1D5DB] hover:bg-white/5 focus-visible:outline-2 focus-visible:outline-[#22C55E]">Logout</button></form></div></details>; }
