import type { ReactNode } from "react";
export interface TimelineItem { id: string; title: string; description?: string; timestamp: string; icon?: ReactNode }
export function Timeline({ items, empty }: { items: TimelineItem[]; empty?: ReactNode }) { if (!items.length) return <>{empty ?? <p className="text-sm text-[#9CA3AF]">No activity yet.</p>}</>; return <ol className="relative border-l border-white/10 pl-6">{items.map((item) => <li key={item.id} className="relative pb-6 last:pb-0"><span className="absolute -left-[1.95rem] flex h-4 w-4 items-center justify-center rounded-full bg-[#22C55E] ring-4 ring-[#161A34]">{item.icon}</span><time className="text-xs text-[#9CA3AF]">{item.timestamp}</time><h3 className="mt-1 text-sm font-semibold text-[#E5E7EB]">{item.title}</h3>{item.description ? <p className="mt-1 text-sm text-[#9CA3AF]">{item.description}</p> : null}</li>)}</ol>; }
