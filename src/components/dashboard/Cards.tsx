import type { ReactNode } from "react"; import { componentClasses } from "@/src/design-system";
export function DashboardCard({ title, description, action, children }: { title: string; description?: string; action?: ReactNode; children: ReactNode }) { return <section className={`${componentClasses.surface} p-5`}><div className="flex items-start justify-between gap-4"><div><h3 className="font-semibold text-[#E5E7EB]">{title}</h3>{description ? <p className="mt-1 text-sm text-[#9CA3AF]">{description}</p> : null}</div>{action}</div><div className="mt-5">{children}</div></section>; }
export function KpiCard(props: { title: string; value: ReactNode; trend?: string; icon?: ReactNode }) { return <DashboardCard title={props.title} action={props.icon}><p className="text-2xl font-bold text-[#E5E7EB]">{props.value}</p>{props.trend ? <p className="mt-2 text-xs text-[#9CA3AF]">{props.trend}</p> : null}</DashboardCard>; }
export function QuickActionCard({ title, description, icon, action }: { title: string; description: string; icon?: ReactNode; action: ReactNode }) { return <DashboardCard title={title} description={description} action={icon}>{action}</DashboardCard>; }
export const ActivityCard = DashboardCard; export const DashboardTimelineCard = DashboardCard; export const DashboardNotificationCard = DashboardCard; export const DashboardCompanyCard = DashboardCard; export const DashboardEmployeeCard = DashboardCard; export const DashboardProjectCard = DashboardCard;
