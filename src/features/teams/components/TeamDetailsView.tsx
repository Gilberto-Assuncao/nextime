import Link from"next/link";import{DashboardCard,KpiCard}from"@/src/components/dashboard";import{TeamStatusBadge}from"./TeamStatusBadge";import{TeamMembersManager}from"./TeamMembersManager";import{TeamDangerZone}from"./TeamDangerZone";import type{TeamDetails}from"../types";export function TeamDetailsView({team}:{team:TeamDetails}){const canManage=team.permissions.includes("manage");const supervisors=team.members.filter(member=>member.role==="supervisor").length;return <div className="space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-sm font-semibold text-[#22C55E]">{team.companyName} · Team</p><h1 className="mt-1 text-2xl font-bold sm:text-3xl">{team.name}</h1><p className="mt-2 max-w-2xl text-sm text-[#9CA3AF]">{team.description||"No description provided."}</p></div><div className="flex items-center gap-3"><TeamStatusBadge status={team.status}/>{canManage?<Link href={`/dashboard/teams/${team.id}/edit`} className="inline-flex min-h-11 items-center rounded-lg border border-white/15 px-4 font-semibold">Edit team</Link>:null}</div></header><section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><KpiCard title="Members" value={team.members.length}/><KpiCard title="Leader" value={team.leaderName??"Unassigned"}/><KpiCard title="Supervisors" value={supervisors}/><KpiCard title="Status" value={<span className="capitalize">{team.status}</span>}/></section><DashboardCard title="Overview"><dl className="grid gap-4 text-sm sm:grid-cols-2 lg:grid-cols-4"><div><dt className="text-[#9CA3AF]">Company</dt><dd>{team.companyName}</dd></div><div><dt className="text-[#9CA3AF]">Created</dt><dd>{new Date(team.createdAt).toLocaleDateString("en-BE")}</dd></div><div><dt className="text-[#9CA3AF]">Updated</dt><dd>{new Date(team.updatedAt).toLocaleDateString("en-BE")}</dd></div><div><dt className="text-[#9CA3AF]">Color</dt><dd><span className="mr-2 inline-block h-3 w-3 rounded-full" style={{backgroundColor:team.color}}/>{team.color}</dd></div></dl></DashboardCard><TeamMembersManager teamId={team.id} members={team.members} available={team.availableMembers} canManage={canManage&&team.status!=="archived"}/><TeamDangerZone teamId={team.id} status={team.status} canArchive={team.permissions.includes("archive")}/></div>}
