import type { TeamActionState,TeamFormValues,TeamStatus } from "./types";
const uuid=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
function text(data:FormData,key:string,max:number){const value=data.get(key);return typeof value==="string"?value.trim().replace(/\s+/g," ").slice(0,max):"";}
export function validateTeamForm(formData:FormData){const memberIds=formData.getAll("memberIds").filter((value):value is string=>typeof value==="string"&&value.length>0);const data:TeamFormValues={name:text(formData,"name",100),description:text(formData,"description",500),status:text(formData,"status",16) as TeamStatus,color:text(formData,"color",7),icon:text(formData,"icon",32),leaderMembershipId:text(formData,"leaderMembershipId",36),memberIds};const fieldErrors:NonNullable<TeamActionState["fieldErrors"]>={};if(data.name.length<2)fieldErrors.name="Use between 2 and 100 characters.";if(!["active","inactive","archived"].includes(data.status))fieldErrors.status="Select a valid status.";if(data.color&&!/^#[0-9a-f]{6}$/i.test(data.color))fieldErrors.color="Use a six-digit hex color.";if(data.leaderMembershipId&&!uuid.test(data.leaderMembershipId))fieldErrors.leaderMembershipId="Select a valid leader.";if(memberIds.some(id=>!uuid.test(id))||new Set(memberIds).size!==memberIds.length)fieldErrors.memberIds="Members must be valid and unique.";return Object.keys(fieldErrors).length?{error:{status:"error",message:"Review the highlighted fields.",fieldErrors} as TeamActionState}:{data};}
