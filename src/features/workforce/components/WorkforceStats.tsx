import { MetricCard } from "@/src/components/data-display"; import type { WorkforceMemberView, WorkforceTeamView } from "../types";
export function WorkforceStats({ members, teams }: { members:WorkforceMemberView[]; teams:WorkforceTeamView[] }) { const active=members.filter(member=>member.membershipStatus==="active").length; const invited=members.filter(member=>member.membershipStatus==="invited").length; return <dl className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Total members" value={members.length} detail="Demonstration workspace"/><MetricCard label="Active members" value={active} detail="Current memberships"/><MetricCard label="Teams" value={teams.length} detail="Active and inactive"/><MetricCard label="Pending invites" value={invited} detail="Visual preview only"/></dl>; }
