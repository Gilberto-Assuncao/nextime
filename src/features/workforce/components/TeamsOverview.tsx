import { EntityCard } from "@/src/components/cards"; import { Badge } from "@/src/components/data-display"; import { Button } from "@/src/components/ui"; import type { WorkforceTeamView } from "../types";
export function TeamsOverview({ teams }: { teams:WorkforceTeamView[] }) { return <section aria-labelledby="teams-heading"><div className="mb-4"><h2 id="teams-heading" className="text-xl font-semibold text-[#E5E7EB]">Teams</h2><p className="mt-1 text-sm text-[#9CA3AF]">Team structure for the current company.</p></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{teams.map(team=><EntityCard key={team.id} title={team.name} subtitle={team.description} status={team.status} metadata={<dl className="grid gap-2"><div><dt className="text-xs text-[#9CA3AF]">Leader</dt><dd>{team.leader}</dd></div><div><dt className="text-xs text-[#9CA3AF]">Members</dt><dd>{team.memberCount}</dd></div></dl>} action={<Button variant="outline" className="w-full">View team</Button>}>{team.currentUserRole?<Badge>My role: {team.currentUserRole}</Badge>:null}</EntityCard>)}</div></section>; }
